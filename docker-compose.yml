services:
  osint-canvas:
    build: .
    ports:
      - "3001:3001"
    volumes:
      # Persist saved scripts across container restarts
      - osint-scripts:/app/data
    restart: unless-stopped
    environment:
      - PORT=3001
      - DATA_DIR=/app/data

volumes:
  osint-scripts:
